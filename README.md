# vite-css-module-comment-bug
1. Run `npm run build` to see the error.
2. Open `src/index.module.scss` and remove multiline comment inside of `:global` block/
3. Run `npm run build` again to see no error.
